---
- hosts: localhost
  gather_facts: false
  tasks:
    - name: Get AAP's outbound IP
      ansible.builtin.uri:
        url: https://ifconfig.me/ip
        return_content: true
      register: result

    - name: Print it
      debug:
        msg: "{{ result.content }}"
